// Canonical BARKVERSE help assistant is initialized by app/analytics.js.
// This legacy file intentionally contains no UI code so it cannot create a duplicate helper.
// Kept as a compatibility stub because older deployments still reference this script.
