const photo=document.querySelector('#dogPhoto');const preview=document.querySelector('#preview');const demoBtn=document.querySelector('#demoBtn');const world=document.querySelector('#world');const names=['Bruno','Luna','Milo','Bella','Max','Coco','Rocky','Daisy'];const jobs=['Chief Sofa Security Officer','Minister of Emergency Zoomies','Director of Snack Intelligence','Head of Unauthorized Cuddles','Commissioner of Backyard Security','Senior Sock Investigation Officer'];let selectedName='Bruno';photo.addEventListener('change',()=>{const file=photo.files?.[0];if(!file)return;const url=URL.createObjectURL(file);preview.innerHTML=`<img src="${url}" alt="Uploaded dog">`;preview.classList.remove('hidden');demoBtn.textContent='Discover my dog →'});demoBtn.addEventListener('click',()=>{selectedName=names[Math.floor(Math.random()*names.length)];document.querySelector('#dogName').textContent=selectedName;document.querySelector('#dogNameTop').textContent=`${selectedName.toUpperCase()} NETWORK`;document.querySelector('#occupation').textContent=jobs[Math.floor(Math.random()*jobs.length)];world.classList.remove('hidden');world.scrollIntoView({behavior:'smooth'})});document.querySelector('#caseBtn').addEventListener('click',()=>{alert(`${selectedName} has been cleared of all charges. The biscuit was found inside ${selectedName}'s stomach. Investigation closed.`)});document.querySelector('#talkBtn').addEventListener('click',()=>{const replies=[`I investigated the sofa. It belongs to me now.`,`I did not eat the biscuit. I merely relocated it internally.`,`You went to work without me. I have opened a formal complaint.`,`The cat knows too much. We should discuss this privately.`];document.querySelector('#dogReply').textContent=replies[Math.floor(Math.random()*replies.length)]});document.querySelector('#memoryBtn').addEventListener('click',()=>{const value=document.querySelector('#memoryInput').value.trim();document.querySelector('#memoryOutput').textContent=value?`${selectedName} will remember this forever. Some jobs are bigger than the dog internet.`:'Tell us one little thing you love about your dog first.'});
